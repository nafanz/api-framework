# Phython based framework

Steps to install
1. Install dependencies ```pip install -r requirements.txt```
2. Update package manager ```pip install --upgrade pip```
3. With the installation complete run ```py.test```
